server:
  port: 9002
  error:
    include-message: always

spring:
  application:
    name: SERVER-PROJECT
  datasource:
    url: jdbc:postgresql://localhost:5432/app61cat
    username: postgres
    password: postgres
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
      show-sql: true